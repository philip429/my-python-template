project_name:
  type: str
  help: What is the name of your project? (Use underscores, e.g., my_project)
  default: default_project

python_version:
  type: str
  help: Which Python version to use?
  default: "3.12"

_tasks:
  # Synchronize Python version and dependencies
  - git init
  - uv sync
